# This workflow will build a project with Gradle
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-gradle

name: CI with Gradle

on:
  [ push, pull_request ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin' # See 'Supported distributions' for available options
        java-version: '21'
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Grant write permission for database
      run: chmod a+w ./database/piradio.*
    - name: Check permissions
      run: ls -l ./database/piradio.*
    - name: Build with Gradle
      run: ./gradlew build -x test
#   dependency check not compatible with java 21.0.1
#    - name: Build dependencyCheck
#      run: ./gradlew dependencyCheckAnalyze
